import { ContentlimitPipe } from './contentlimit.pipe';

describe('ContentlimitPipe', () => {
  it('create an instance', () => {
    const pipe = new ContentlimitPipe();
    expect(pipe).toBeTruthy();
  });
});
